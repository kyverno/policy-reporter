{{- if .Values.plugin.vap.enabled -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "vap-plugin.fullname" . }}
  namespace: {{ include "policyreporter.namespace" . }}
  labels:
    {{- include "vap-plugin.labels" . | nindent 4 }}
type: Opaque
data:
  config.yaml: {{ tpl (.Files.Get "configs/vap-plugin.tmpl") . | b64enc }}
{{- end }}
