{{- if .Values.ui.enabled -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ui.fullname" . }}-config
  labels:
    {{- include "ui.labels" . | nindent 4 }}
type: Opaque
data:
  config.yaml: {{ tpl (.Files.Get "config-ui.tmpl") . | b64enc }}
{{- end }}